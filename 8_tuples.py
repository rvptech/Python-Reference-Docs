# Tuple is immutable unlike list

teaData = ("lemon", "black", "green")

print(teaData[0]) # slicing works same as that of lists.

print(len(teaData)) # length of the items in the tuple

# remaining conditional and loops works same as that of lists

